import { ValidateUsStateDirective } from './validate-us-state.directive';

describe('ValidateUsStateDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateUsStateDirective();
    expect(directive).toBeTruthy();
  });
});
